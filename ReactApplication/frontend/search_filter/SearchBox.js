import React from 'react';

const SearchBox = (props) =>{
    return(
        <input type='search'
        className='search'
        placeholder={}
        onChange = {y
        }
        />
    )
}

export default SearchBox;